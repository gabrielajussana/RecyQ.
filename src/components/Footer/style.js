import styled from 'styled-components/macro';
import tw from 'twin.macro';

const Container = styled.div`
  ${tw`flex flex-col items-center bg-slate-100 justify-center py-4 font-semibold`}
`;

export { Container};